	data "Global Thermonuclear War"
	if a$ = chr$(i) then

		' You probably thought this was a real game, huh?
		attr 0, 0
		cls
		locate 5, 3
		print "          How about a nice game of chess?"
		locate 5, 8
		print "A strange game.  The only way to win is not to play."

		' A short delay
		for j = 1 to 70
			x = sin(1)
		next j

		' Return to the kiosk menu
		run

	end if
	i = i + 1
